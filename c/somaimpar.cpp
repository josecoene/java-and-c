#include <stdio.h>

int main() {

    int inicio, fim, soma, aux;

    scanf("%d", &inicio); // ler a variavel de inicio
    scanf("%d", &fim); // ler a variavel de fim

    if (inicio / 2 != 0){

    }
    for (inicio; fim; ++soma) {
        if (soma / 2 != 0) {
            soma++;
        }
    }

    printf("%d", soma);
    
    return 0;
}